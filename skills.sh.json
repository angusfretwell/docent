{
  "$schema": "https://skills.sh/schemas/skills.sh.schema.json",
  "notGrouped": "bottom",
  "groupings": [
    {
      "title": "Review loop",
      "description": "The skill you invoke in your own agent session to review a branch, write its code and product walkthroughs (`/docent`), pull Comments (`/docent --read`), and record outcomes (`/docent --write`).",
      "skills": ["docent"]
    }
  ]
}
