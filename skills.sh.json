{
  "$schema": "https://skills.sh/schemas/skills.sh.schema.json",
  "notGrouped": "bottom",
  "groupings": [
    {
      "title": "Review loop",
      "description": "The skill you invoke in your own agent session to review, fix, and reconcile a branch under review — walkthrough reconciliation (`/docent`), pulling Comments (`/docent --read`), and recording outcomes (`/docent --write`).",
      "skills": ["docent"]
    }
  ]
}
