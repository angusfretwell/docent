# Palette

A tiny static color-palette toy. Serve the directory and open `index.html` to
render the preset swatches — no build step, no dependencies.

## Layout

- `index.html` — the page shell
- `styles.css` — swatch grid and typography
- `app.js` — the preset palette and rendering
- `color.js` — hex/channel conversion and contrast
