/**
 * Prose and callout classes shared by the narrated surfaces: section prose
 * and the finding callout rail render identically on the code and product
 * walkthrough views.
 */

export const proseClass = "my-2 leading-normal whitespace-pre-wrap break-words";

export const calloutClass =
  "my-1.5 border-l-2 px-2.5 py-0.5 text-sm whitespace-pre-wrap break-words";

export const findingClass = `${calloutClass} border-l-info/50`;
